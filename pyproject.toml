[project]
name = "pyro-spark"
version = "0.1.0"
description = "PySpark and notebook development environment"
requires-python = ">=3.12"
dependencies = [
    "delta-spark>=4.0",
    "ipykernel>=7.2.0",
    "jupyterlab>=4.5.6",
    "pyspark>=4.1.1",
]
